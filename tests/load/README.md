# Load ladder placeholders

Measurement artifacts from:

```bash
python scripts/fleet_simulator.py --ladder --json > tests/load/1k/result.json   # after editing sizes
python scripts/fleet_simulator.py --extended-ladder --json
python scripts/realtime_load_test.py --ladder --persist
```

Do not commit fabricated numbers. See `docs/ops/CAPACITY-LAB.md`.
