# Load / capacity ladder notes
#
# These directories hold measurement artifacts (JSON summaries) from:
#   python scripts/fleet_simulator.py --ladder --json
#   python scripts/realtime_load_test.py --ladder
#
# Do NOT claim 5K / 10K / 100K production capacity until the matching
# docs/ops/CAPACITY-LAB.md row is filled with measured hardware + Redis +
# Postgres figures from an owned lab.
#
# Suggested progression: 1k → 5k → 10k → 25k → 50k → 100k
