# Dev/test-only dependencies — install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
# Standalone binary — same spec (packaging/securaiq.spec), OS-specific launcher:
#   Windows:     .\build_exe.cmd        (or scripts\build_exe.ps1)
#   Linux/macOS: ./build_exe.sh         (or scripts/build_exe.sh)
# Build ON the OS you want a binary for — PyInstaller does not cross-compile.
pyinstaller>=6.3
