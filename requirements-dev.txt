# Dev/test-only dependencies — install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
# Windows EXE: pip install pyinstaller && .\\build_exe.cmd
pyinstaller>=6.3
