apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: securaiq
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
