fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0
chromadb>=0.4.22
sentence-transformers>=2.3.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
cryptography>=42.0.0
argon2-cffi>=23.1.0

# Optional: local HuggingFace inference
# torch>=2.1.0
# transformers>=4.38.0
# accelerate>=0.27.0
# bitsandbytes>=0.42.0

# Optional: LoRA fine-tuning
# peft>=0.9.0
# trl>=0.7.0
# datasets>=2.17.0

# Optional: Unsloth (GPU recommended) — https://github.com/unslothai/unsloth
# pip install unsloth
# pip install datasets trl peft accelerate bitsandbytes

# Optional: error reporting (set SENTRY_DSN to enable — see app/error_reporting.py)
# sentry-sdk>=2.0.0

# Optional: Postgres dual-backend (set DATABASE_URL=postgresql://...)
# psycopg[binary]>=3.1.0

# Optional: Alembic migrations for SaaS Postgres (scripts/db_migrate_and_run.py)
# alembic>=1.13.0
# sqlalchemy>=2.0.0

# Optional: multi-worker realtime bus (set REDIS_URL)
# redis>=5.0.0

# Optional: Prefect job orchestration (set PREFECT_ENABLED=true)
# prefect>=3.0,<4

# Optional: SAML ACS signature verification (#256) — required before accepting IdP assertions
# Install with: pip install -r requirements-saml.txt
# signxml>=3.2.0
# lxml>=5.0.0
